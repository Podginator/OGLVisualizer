#pragma once

#include <math.h>
//When creating this we only want a Vector of up to 4. 
template<typename T>
struct VecBase
{
	//Common stuff
};


template <size_t size, typename T>
struct Vector
{
private:
	T data[n];
};